import { FC } from "react";
import Timer from "./Timer";

const TimerPage: FC = () => {
    return (
        <div className="main-timer">
            <Timer/>
        </div>
    )
}

export default TimerPage